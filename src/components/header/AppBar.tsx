import React from 'react';
import {Layout} from 'antd';

const {Header} = Layout;

const AppBar = () => {
    return <Layout>
        <Header></Header>
    </Layout>
};

export default AppBar;